Desktop metaphor? server's os? mobile os?

- os is built from the kernel and os applications layer:
-- kernel talks to the hardware through drivers


windows:



a small tweak to the mapping(if not present):
-GUI Desktopping: focus on visuals - taskbar(and dfs from there), desktop background (and dfs from there) etc..
-Terminal Desktopping: batch(+posh), bash



















merge this to the below portion 
& & & & & & Windows NT Desktopping SCs & & & & & &

^ ^ ^ ^ ^ Windows 10 Navigation ^ ^ ^ ^ ^ 

move this - windows versions should contian relatively little stuff, but to where?
1. Launch Run command: -> super + r
2. Launch TaskManager: -> ctrl + shft + esc
3. Launch n-th pinned Icon in TaskBar: ->super+<Number>
4. Round-Robin focus between open windows: ->alt + tab
5. Round-Robin focus between open windows in reverse: ->shft + alt + tab
6. Open active/open window menu: ->super + tab 
7.


























& & & & & & Windows Desktopping & & & & & &

^ ^ ^ ^ ^ Desktop Background ^ ^ ^ ^ ^

^ ^ ^ ^ Icon Handling ^ ^ ^ ^


^ ^ ^ ^ ^ Task Bar ^ ^ ^ ^ ^


^ ^ ^ ^ Start Menu Button ^ ^ ^ ^


^ ^ ^ ^ Search Box ^ ^ ^ ^


^ ^ ^ ^ Pinned Section (Right & Middle Section) ^ ^ ^ ^


^ ^ ^ ^ System Tray/Hidden Icons (Right Section) ^ ^ ^ ^


^ ^ ^ ^ Notification area ^ ^ ^ ^

















& & & & & & Linux Desktopping & & & & & &
ubuntu:
Control Panel (left-side bar)
Menu Bar(pops up in frame handling to the left in the upper bar, like app menu in macos)
Task Bar (right side of the menu bar)

debian:

// Shell commands and shortcuts:
1. whereis
2. whatis

--- stopped at wget ---

// &&&&&& App launcher &&&&&&
// ^^^ General ^^^
1. Terminal: -xfc4-terminal -> CTRL+ALT+T
2. Task Manager: -xfce4-taskmanager -> CTRL+ALT+S
3. Mousepad: -mousepad -> CTRL+ALT+M
4. FireFox: 
5.
6.


non finder able
1.
2.
3.

Navigation 

-- stopped at setting editor--

Non finder-able

Menus:
1.
2.
3.









& & & & & & MacOs DeskTopping & & & & & &

^ ^ ^ ^ ^ Desktop Background ^ ^ ^ ^ ^


^ ^ ^ ^ ^ Menu Bar ^ ^ ^ ^ ^

^ ^ ^ ^ System Control (Left Section) ^ ^ ^ ^

^ ^ ^ Apple Menu Button ^ ^ ^


^ ^ ^ ^ App Menu (Center Section) ^ ^ ^ ^


^ ^ ^ ^ Status Menu (Right Section) ^ ^ ^ ^

^ ^ ^ Notification Center ^ ^ ^


^ ^ ^ ^ ^ Dock ^ ^ ^ ^ ^













win file systems

git files

.git directory
- a directory that grows as the project continues
- .git/log is the directory that saves the changes of the directory

.gitignoe directory
- leaves out files for staging 

.gitconfig directory
- configuration information























os wraps the kernel
-for ease of use
-for managing tasks(os proccesses more easily)






- components? roll overing components through future releaes?
- file systems? os native directories? (like windows tools?)`
- os pre installed software?
desktop shortcuts?
what else?
